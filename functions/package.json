{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "yarn build && firebase serve --only functions",
    "test": "nyc mocha -r ts-node/register test/*.test.ts",
    "deploy:dev": "yarn build && firebase use default && firebase deploy --only functions",
    "deploy:prod": "yarn build && firebase use prod && firebase deploy --only functions"
  },
  "engines": {
    "node": "10"
  },
  "main": "lib/index.js",
  "dependencies": {
    "axios": "^0.19.2",
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0",
    "telegraf": "^3.36.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.8",
    "@types/mocha": "^7.0.1",
    "@types/sinon": "^7.5.1",
    "chai": "^4.2.0",
    "firebase-functions-test": "^0.1.6",
    "mocha": "^7.0.1",
    "nyc": "^15.0.0",
    "sinon": "^8.1.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.2.2"
  },
  "private": true
}
