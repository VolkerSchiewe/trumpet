{
  "name": "trumpet",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "start": "parcel src/index.html",
    "clean": "rm -rf .cache dist",
    "build": "parcel build src/index.html --detailed-report --public-url '.'",
    "deploy:local": "yarn clean && yarn build && firebase serve",
    "deploy:dev": "yarn clean && yarn build && firebase use default && firebase deploy",
    "deploy:prod": "yarn clean && yarn build --no-source-maps && firebase use prod && firebase deploy"
  },
  "dependencies": {
    "@material-ui/core": "^4.1.3",
    "@material-ui/icons": "^4.2.1",
    "express": "^4.17.1",
    "firebase-functions": "^3.3.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "@types/react": "^16.8.22",
    "@types/react-dom": "^16.8.4",
    "@types/workbox-sw": "^4.2.0",
    "firebase-tools": "^7.0.2",
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.5.2"
  }
}
